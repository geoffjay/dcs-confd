// TODO: Clean up imports using this file, needs to be tested before switch

import ConfigurationModel from './configuration'
import ObjectModel from './object'
import PropertyModel from './property'
import PluginModel from './plugin'

export default { ConfigurationModel, ObjectModel, PropertyModel, PluginModel }
